{
  "name": "myyoutube",
  "module": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "^3.1.1",
    "rimraf": "^5.0.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "clean": "rimraf dist",
    "start": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --compile --outfile dist/myyoutube"
  },
  "dependencies": {
    "axios": "^1.6.2"
  }
}
